# crowd-funding-platform
**important note:-**
make sure to use the code in localhost to add the donation data into database, otherwise the donation will be added but it won't show in database records.
you can run the code with out using local host in php, just open using index file.
